/**
 * Created by leiyang on 2024/3/30 21:33
 */
#include <bits/stdc++.h>
using namespace std;

int main(){
    int q, k, p;
    cin>>q;
    while(q--) {
        cin>>k>>p;
        // 循环节为k，分母为p
        // 思路小于p的所有值，首先判断是否能变成无限循环小数，再对其进行实际的枚举判断
        for(int i = 1;i<p;i++) {

        }
    }
    return 0;
}
